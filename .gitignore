.DS_Store
*.tmp
export/

# Corpus for the cs/ project IS versioned (author's own research + Glitch
# materials) so it syncs between the author and the agent session.
# en/ kit corpus stays local (copyright): only the empty folders are versioned.
en/raw_primary/*
en/raw_secondary/*
!en/raw_primary/.gitkeep
!en/raw_secondary/.gitkeep
